/**
 * Padding values that can be used with UI modifiers.
 * These are strictly numeric (no percentages or strings allowed).
 */
export type PaddingStyle = {
  padding?: number;
  paddingHorizontal?: number;
  paddingVertical?: number;
  paddingTop?: number;
  paddingBottom?: number;
  paddingLeft?: number;
  paddingRight?: number;
};
