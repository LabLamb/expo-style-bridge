import { create } from "./create";
import { compose } from "./compose";
import { flatten } from "./flatten";
import { absoluteFill } from "./constants";

export const NativeStyleSheet = {
  create,
  compose,
  flatten,
  absoluteFill,
};

export type { NativeStyleDescriptor } from "./descriptor";
